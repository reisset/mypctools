# myscreensavers configuration file
# Copy to: ~/.config/myscreensavers/config
#
# All settings are optional. Uncomment and modify as needed.

# ─────────────────────────────────────────────────────────────
# DAEMON SETTINGS (screensaver-daemon)
# ─────────────────────────────────────────────────────────────

# Idle timeout before screensaver starts (milliseconds)
# Default: 300000 (5 minutes)
#IDLE_TIMEOUT_MS=300000

# How often to check idle state (seconds, polling mode only)
# Default: 5
#CHECK_INTERVAL=5

# Preferred terminal emulator (auto-detected if not set)
# Options: kitty, ghostty, foot, alacritty, gnome-terminal
#PREFERRED_TERMINAL=kitty

# Lock screen before starting screensaver (provides actual security)
# Default: false
#LOCK_ON_IDLE=true

# ─────────────────────────────────────────────────────────────
# SCREENSAVER SETTINGS (bin/screensaver)
# ─────────────────────────────────────────────────────────────

# Custom ASCII art directory (defaults to project's config/ascii_art)
#ART_DIR=/path/to/your/ascii_art

# Custom effects list (overrides built-in selection)
# Run `tte --help` to see all available effects
#EFFECTS=(
#    matrix
#    rain
#    blackhole
#    fireworks
#)
